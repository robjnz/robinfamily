import { IconifyJSON } from '@iconify/types';
import { PreprocessorGroup } from 'svelte/types/compiler/preprocess';

interface Options {
    silent?: boolean;
    mode?: 'development' | 'production';
    configPath?: string;
    disableFormat?: boolean;
    devTools?: {
        enabled: boolean;
        completions?: boolean;
    };
    safeList?: string;
    preflights?: boolean;
    experimental?: {
        icons?: {
            prefix?: string;
            collections?: Record<string, IconifyJSON>;
            extraProperties?: Record<string, string>;
        };
    };
}
declare let OPTIONS: Options;
declare function windi(options?: typeof OPTIONS): PreprocessorGroup;

export { Options, windi as default, windi };
